name: "test"
on: push
jobs:
  deploy:
    name: Deploy to EC2
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Go app directory
        env:
          EC2_SSH:  ${{ secrets.aws_ec2_ssh_key}} 
        run: |
            cd my-blog
            ls
            echo "%EC2_SSH%"